<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <title>Chỉnh Sửa Sản Phẩm</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f0f2f5; }
        .card { border: none; border-radius: 20px; box-shadow: 0 10px 25px rgba(0,0,0,0.05); }
        .current-img { width: 100%; max-height: 250px; object-fit: contain; border-radius: 15px; background: #fff; padding: 10px; border: 1px solid #eee; }
        .form-control { border-radius: 10px; padding: 12px; }
    </style>
</head>
<body class="py-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card p-4 p-md-5">
                    <div class="row g-4">
                        <div class="col-md-5 text-center">
                            <h5 class="fw-bold mb-3">Ảnh hiện tại</h5>
                            <img src="<%= product.url_image %>" class="current-img mb-3" alt="Current image">
                            <p class="text-muted small">Nếu không chọn ảnh mới, hệ thống sẽ giữ lại ảnh này.</p>
                        </div>
                        <div class="col-md-7 border-start ps-md-4">
                            <h2 class="fw-bold mb-4 text-primary">Cập Nhật Thông Tin</h2>
                            <form action="/edit" method="POST" enctype="multipart/form-data">
                                <input type="hidden" name="id" value="<%= product.id %>">
                                <input type="hidden" name="old_image_url" value="<%= product.url_image %>">
                                
                                <div class="mb-3">
                                    <label class="form-label fw-bold">Tên sản phẩm</label>
                                    <input type="text" name="name" class="form-control" value="<%= product.name %>" required>
                                </div>
                                <div class="row mb-3">
                                    <div class="col-6">
                                        <label class="form-label fw-bold">Giá (VND)</label>
                                        <input type="number" name="price" class="form-control" value="<%= product.price %>" required>
                                    </div>
                                    <div class="col-6">
                                        <label class="form-label fw-bold">Số lượng</label>
                                        <input type="number" name="quantity" class="form-control" value="<%= product.quantity %>" required>
                                    </div>
                                </div>
                                <div class="mb-4">
                                    <label class="form-label fw-bold">Thay đổi hình ảnh mới</label>
                                    <input type="file" name="image" class="form-control" accept="image/*">
                                </div>
                                <div class="d-grid gap-2">
                                    <button type="submit" class="btn btn-warning py-3 fw-bold shadow-sm">Xác nhận cập nhật</button>
                                    <a href="/" class="btn btn-light border">Hủy và quay lại</a>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>