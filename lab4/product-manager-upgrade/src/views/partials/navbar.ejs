<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="/">AWS Product Manager</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav me-auto">
                <li class="nav-item"><a class="nav-link" href="/">Sản phẩm</a></li>
                <% if(user && user.role === 'admin') { %>
                    <li class="nav-item"><a class="nav-link" href="/categories">Danh mục</a></li>
                <% } %>
            </ul>
            <span class="navbar-text text-white me-3">
                Xin chào, <strong><%= user ? user.username : 'Guest' %></strong>
            </span>
            <a href="/logout" class="btn btn-sm btn-outline-light">Đăng xuất</a>
        </div>
    </div>
</nav>