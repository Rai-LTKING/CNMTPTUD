<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sửa Sản Phẩm - Product Manager</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body { background-color: #f8f9fa; }
        .card { border: none; border-top: 4px solid #ffc107; }
        .preview-img { max-width: 100%; max-height: 200px; object-fit: contain; border-radius: 8px; margin-top: 10px; border: 1px solid #dee2e6; }
    </style>
</head>
<body>

<nav class="navbar navbar-dark bg-dark mb-5">
    <div class="container">
        <a href="/" class="navbar-brand mb-0 h1"><i class="bi bi-box-seam"></i> Product Manager</a>
    </div>
</nav>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <div class="card shadow-sm">
                <div class="card-header bg-white py-3">
                    <h5 class="card-title mb-0 text-dark">
                        <i class="bi bi-pencil-square text-warning"></i> Chỉnh sửa thông tin sản phẩm
                    </h5>
                </div>
                <div class="card-body p-4">
                    <form action="/update" method="POST">
                        <div class="mb-3">
                            <label class="form-label fw-bold">ID Sản phẩm (Không thể sửa)</label>
                            <input type="text" name="id" class="form-control bg-light" value="<%= product.id %>" readonly>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Tên sản phẩm</label>
                            <input type="text" name="name" class="form-control" value="<%= product.name %>" required placeholder="Nhập tên sản phẩm">
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Giá bán (VND)</label>
                            <div class="input-group">
                                <input type="number" name="price" class="form-control" value="<%= product.price %>" required>
                                <span class="input-group-text">₫</span>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Link ảnh sản phẩm</label>
                            <input type="text" name="url_image" class="form-control" value="<%= product.url_image %>" placeholder="https://...">
                            <% if (product.url_image) { %>
                                <div class="mt-2 text-center">
                                    <small class="text-muted d-block mb-1">Xem trước ảnh hiện tại:</small>
                                    <img src="<%= product.url_image %>" alt="preview" class="preview-img">
                                </div>
                            <% } %>
                        </div>

                        <hr class="my-4">

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-warning flex-grow-1">
                                <i class="bi bi-check-lg"></i> Lưu thay đổi
                            </button>
                            <a href="/" class="btn btn-outline-secondary px-4">
                                Hủy bỏ
                            </a>
                        </div>
                    </form>
                </div>
            </div>
            
            <div class="text-center mt-3 text-muted">
                <small>Dữ liệu được lưu trữ trực tiếp trên DynamoDB Local</small>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>